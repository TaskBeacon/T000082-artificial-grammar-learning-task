# Task Plot Review

## Final decision

PASS after five generation/revision rounds.

## Evidence match

- Task and construct match the canonical folder and README.
- The three representative rows are training, grammatical test, and nongrammatical test.
- Literal strings are correct: `PTVV`, `PVPXVPS`, and `PVPXVVS`.
- Training order is study -> typed reproduction -> conditional neutral retry/re-presentation -> fixation.
- Test order is classification -> fixation, with `F = Conforms`, `J = Violates`, and no correctness feedback.
- Timings are correct: 3.0-s study, 10.0-s per-key recall window, 0.75-s retry notice, three attempts maximum, 300-s block interval, 10.0-s classification maximum, and 0.5-s ITI.
- The one-position violation and lack of test feedback are represented without inventing extra participant-facing phases.

## Visual quality

- Final title, `Construct:` subtitle, and TaskBeacon lockup occupy the reserved header without overlapping the timeline.
- All text and exact letter strings are legible at normal preview size.
- Screen boxes, timing labels, row labels, separators, and arrows do not overlap.
- Horizontal scale uses the landscape width consistently; grammatical and nongrammatical rows align.
- Accent colors are restrained and the diagram remains a scientific workflow figure.
- The raw image contains no generated title, subtitle, logo, watermark, or brand text.

## Iteration record

1. Generated the complete three-row timeline; rejected explicit `Incorrect` feedback and excessive test-row whitespace.
2. Replaced feedback with the neutral re-presentation notice; retained for content but revised spacing.
3. Distributed test screens across the timeline width; rejected because the 300-s interval annotation was lost.
4. Restored the exact `300-s interval`; rejected after watermark preview because the header-safe band was too shallow.
5. Expanded the blank header band and shifted the unchanged timeline downward; final watermarked image passed visual review.

## README embed

- `README.md` contains `## 2. Task Flow`.
- The first image below that heading is exactly `![Task Flow](task_flow.png)`.
- Final output is saved as `task_flow.png`; the pre-watermark source is preserved as `references/task_plot_timeline_raw.png`.
